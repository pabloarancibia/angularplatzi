import { QuantityProductsPipe } from './quantity-products.pipe';

describe('QuantityProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
